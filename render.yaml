services:
  - type: web
    name: ssg-election-app
    runtime: php
    plan: free
    buildCommand: composer install --no-dev
    startCommand: php -S 0.0.0.0:10000 -t api-version1
    envVars:
      - key: PHP_VERSION
        value: "8.2"
      - key: DB_HOST
        value: "153.92.15.54"
